import React from "react";
import { createRoot } from "react-dom/client";
import DriverNetwork from "./DriverNetwork.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <DriverNetwork />
  </React.StrictMode>
);
